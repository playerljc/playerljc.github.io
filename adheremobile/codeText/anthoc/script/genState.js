const fs=require("fs"),path=require("path"),rootPath=path.join(__dirname,"../examples");function loop(t,o){let e="";return t.forEach(((t,o)=>{const n=path.extname(t);if(".less"===n||".css"===n)return;const a=`${path.basename(t).substring(0,t.lastIndexOf("."))}CodeText`;e+=`\n      const [${a}, set${a}] = useState('');\n    `})),e}fs.readdirSync(rootPath).forEach((t=>{console.log(`\n      ${loop(fs.readdirSync(path.join(rootPath,t)),path.join(rootPath,t))}\n  `)}));