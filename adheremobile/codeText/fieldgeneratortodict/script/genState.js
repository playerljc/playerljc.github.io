const fs=require("fs"),path=require("path"),rootPath=path.join(__dirname,"../examples");function loop(t,e){let n="";return t.forEach(((t,e)=>{const a=path.extname(t);if(".less"===a||".css"===a)return;const r=`${path.basename(t).substring(0,t.lastIndexOf("."))}CodeText`;n+=`\n      const [${r}, set${r}] = useState('');\n    `})),n}let res="";fs.readdirSync(rootPath).forEach((t=>{res+=`\n      ${loop(fs.readdirSync(path.join(rootPath,t)),path.join(rootPath,t))}\n  `})),fs.writeFileSync(path.join(__dirname,"genState.txt"),res);