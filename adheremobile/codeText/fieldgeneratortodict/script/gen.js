const fs=require("fs"),path=require("path"),rootPath=path.join(__dirname,"../examples"),comRootPath="@/components/ui/fieldgeneratortodict/examples";let res="";function loop(e,t){e.forEach((e=>{const o=path.basename(e).substring(0,e.lastIndexOf(".")),r=path.join(t,e),a=fs.statSync(r);a.isFile()?res+=`\n          FGTD${o},\n        `:a.isDirectory()&&loop(fs.readdirSync(r),r)}))}loop(fs.readdirSync(rootPath),rootPath),fs.writeFileSync(path.join(__dirname,"gen.txt"),res.replace(/\\+/gim,"/"));